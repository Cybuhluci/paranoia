using UnityEngine;

public class PlayerInventory : MonoBehaviour
{
    public int getAmmo(CalibreSO calibre)
    {
        return 500;
        // this will later be much more complex, but for now we will just return 500 for all calibres so that we can test the shooting system
    }
}
